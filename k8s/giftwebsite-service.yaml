apiVersion: v1
kind: Service
metadata:
  name: giftwebsite-service
spec:
  type: NodePort
  selector:
    app: giftwebsite
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
      nodePort: 30900